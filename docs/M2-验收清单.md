# M2 移动端·候选人登录 · 验收清单

> 依据 AGENTS.md「模块开发 Loop」。验收方式：手机模式打开 http://localhost:8090/m.html（浏览器 F12 切手机视口或直接手机访问局域网）。
> 代码在 `feature/M2-mobile-login` 分支，验收通过后合入 master。

**演示约定**：验证码固定 `123456`；权限白名单：13800000001 / 13800000002 / 13800000003（其余号码真实逻辑判为无权限）。

| # | 验收点 | 预期结果 | 通过 |
|---|---|---|---|
| 1 | 移动端布局 | 单列手机宽度布局（PC 打开居中呈手机宽），触控尺寸输入框 | ☐ |
| 2 | 手机号校验 | 非 11 位手机号点登录 → 红色 toast 提示 | ☐ |
| 3 | 错误验证码 | 输 999999 → 提示"验证码错误（演示环境为 123456）"，不弹窗 | ☐ |
| 4 | 获取验证码 | 点击 → toast"验证码已发送（演示：123456）" | ☐ |
| 5 | 红字声明 | 登录页下拉框区域有完整红字："本页面只做 demo 阶段有无权限分支选择，不是真正的页面" | ☐ |
| 6 | 二次弹窗（仅白名单） | **白名单号码**正确验证码登录 → 弹出分支确认弹窗，含红字声明 + 真实逻辑判定行；**非白名单号码登录不弹窗，直接进入无权限页**（2026-08-21 用户修正） | ☐ |
| 7 | 有权限分支 | 确认进入 → "登录成功"页 + 引导页占位 + 返回登录 | ☐ |
| 8 | 无权限分支（外挂） | 下拉切"无权限"→ 白名单号码 13800000001 也能进"未开通权限"页（覆盖真实判定，且真实逻辑未被改动） | ☐ |
| 9 | 首次开通申请 | 无权限页填姓名+学校提交 → 显示记录卡（手机号/姓名/学校/提交时间） | ☐ |
| 10 | 同号再次进入 | 同一手机号再走无权限分支 → 直接显示上次记录，不再出空表单 | ☐ |
| 11 | PC 端回归 | index.html 教师端/学生端不受影响 | ☐ |
| 12 | 视觉规范 | 药丸主按钮、浅色块横幅、红字错误提示、等宽字体数字 | ☐ |

**不通过记录**（格式：#编号 + 问题描述）：

-

## 自测记录（ZCode，2026-08-21）

1-11 全部通过（浏览器 375×812 视口实测：错误码拦截、弹窗内容、双分支跳转、申请记录、同号返回上次记录、PC 回归 200）。截图已发用户浏览器面板。
